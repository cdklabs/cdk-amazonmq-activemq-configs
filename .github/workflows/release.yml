# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: release
on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      dry_run:
        description: Dry run (skip actual publishing)
        required: false
        type: boolean
      package:
        description: Select specific package to release
        required: false
        type: choice
        options:
          - "*all packages*"
          - "@cdklabs/cdk-amazonmq-activemq-config-v5-15-16"
          - "@cdklabs/cdk-amazonmq-activemq-config-v5-16-7"
          - "@cdklabs/cdk-amazonmq-activemq-config-v5-17-6"
          - "@cdklabs/cdk-amazonmq-activemq-config-v5-18-4"
          - "@cdklabs/cdk-amazonmq-activemq-config-v5-19-1"
        default: "*all packages*"
jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      latest_commit: ${{ steps.git_remote.outputs.latest_commit }}
      publish-cdklabs-cdk-amazonmq-activemq-config-v5-15-16: ${{ steps.check-publish-cdklabs-cdk-amazonmq-activemq-config-v5-15-16.outputs.publish }}
      publish-cdklabs-cdk-amazonmq-activemq-config-v5-16-7: ${{ steps.check-publish-cdklabs-cdk-amazonmq-activemq-config-v5-16-7.outputs.publish }}
      publish-cdklabs-cdk-amazonmq-activemq-config-v5-17-6: ${{ steps.check-publish-cdklabs-cdk-amazonmq-activemq-config-v5-17-6.outputs.publish }}
      publish-cdklabs-cdk-amazonmq-activemq-config-v5-18-4: ${{ steps.check-publish-cdklabs-cdk-amazonmq-activemq-config-v5-18-4.outputs.publish }}
      publish-cdklabs-cdk-amazonmq-activemq-config-v5-19-1: ${{ steps.check-publish-cdklabs-cdk-amazonmq-activemq-config-v5-19-1.outputs.publish }}
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Set git identity
        run: |-
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Install dependencies
        run: yarn install --check-files --frozen-lockfile
      - name: release
        run: npx projen release
      - id: check-publish-cdklabs-cdk-amazonmq-activemq-config-v5-15-16
        run: (git ls-remote -q --exit-code --tags origin $(cat dist/releasetag.txt) && (echo "publish=false" >> $GITHUB_OUTPUT)) || echo "publish=true" >> $GITHUB_OUTPUT
        working-directory: packages/@cdklabs/cdk-amazonmq-activemq-config-v5-15-16
      - id: check-publish-cdklabs-cdk-amazonmq-activemq-config-v5-16-7
        run: (git ls-remote -q --exit-code --tags origin $(cat dist/releasetag.txt) && (echo "publish=false" >> $GITHUB_OUTPUT)) || echo "publish=true" >> $GITHUB_OUTPUT
        working-directory: packages/@cdklabs/cdk-amazonmq-activemq-config-v5-16-7
      - id: check-publish-cdklabs-cdk-amazonmq-activemq-config-v5-17-6
        run: (git ls-remote -q --exit-code --tags origin $(cat dist/releasetag.txt) && (echo "publish=false" >> $GITHUB_OUTPUT)) || echo "publish=true" >> $GITHUB_OUTPUT
        working-directory: packages/@cdklabs/cdk-amazonmq-activemq-config-v5-17-6
      - id: check-publish-cdklabs-cdk-amazonmq-activemq-config-v5-18-4
        run: (git ls-remote -q --exit-code --tags origin $(cat dist/releasetag.txt) && (echo "publish=false" >> $GITHUB_OUTPUT)) || echo "publish=true" >> $GITHUB_OUTPUT
        working-directory: packages/@cdklabs/cdk-amazonmq-activemq-config-v5-18-4
      - id: check-publish-cdklabs-cdk-amazonmq-activemq-config-v5-19-1
        run: (git ls-remote -q --exit-code --tags origin $(cat dist/releasetag.txt) && (echo "publish=false" >> $GITHUB_OUTPUT)) || echo "publish=true" >> $GITHUB_OUTPUT
        working-directory: packages/@cdklabs/cdk-amazonmq-activemq-config-v5-19-1
      - name: Check for new commits
        id: git_remote
        run: echo "latest_commit=$(git ls-remote origin -h ${{ github.ref }} | cut -f1)" >> $GITHUB_OUTPUT
      - name: "@cdklabs/cdk-amazonmq-activemq-config-v5-15-16: Backup artifact permissions"
        if: ${{ steps.git_remote.outputs.latest_commit == github.sha }}
        run: cd dist && getfacl -R . > permissions-backup.acl
        continue-on-error: true
        working-directory: packages/@cdklabs/cdk-amazonmq-activemq-config-v5-15-16
      - name: "@cdklabs/cdk-amazonmq-activemq-config-v5-15-16: Upload artifact"
        if: ${{ steps.git_remote.outputs.latest_commit == github.sha }}
        uses: actions/upload-artifact@v7
        with:
          name: cdklabs-cdk-amazonmq-activemq-config-v5-15-16_build-artifact
          path: packages/@cdklabs/cdk-amazonmq-activemq-config-v5-15-16/dist
          overwrite: true
      - name: "@cdklabs/cdk-amazonmq-activemq-config-v5-16-7: Backup artifact permissions"
        if: ${{ steps.git_remote.outputs.latest_commit == github.sha }}
        run: cd dist && getfacl -R . > permissions-backup.acl
        continue-on-error: true
        working-directory: packages/@cdklabs/cdk-amazonmq-activemq-config-v5-16-7
      - name: "@cdklabs/cdk-amazonmq-activemq-config-v5-16-7: Upload artifact"
        if: ${{ steps.git_remote.outputs.latest_commit == github.sha }}
        uses: actions/upload-artifact@v7
        with:
          name: cdklabs-cdk-amazonmq-activemq-config-v5-16-7_build-artifact
          path: packages/@cdklabs/cdk-amazonmq-activemq-config-v5-16-7/dist
          overwrite: true
      - name: "@cdklabs/cdk-amazonmq-activemq-config-v5-17-6: Backup artifact permissions"
        if: ${{ steps.git_remote.outputs.latest_commit == github.sha }}
        run: cd dist && getfacl -R . > permissions-backup.acl
        continue-on-error: true
        working-directory: packages/@cdklabs/cdk-amazonmq-activemq-config-v5-17-6
      - name: "@cdklabs/cdk-amazonmq-activemq-config-v5-17-6: Upload artifact"
        if: ${{ steps.git_remote.outputs.latest_commit == github.sha }}
        uses: actions/upload-artifact@v7
        with:
          name: cdklabs-cdk-amazonmq-activemq-config-v5-17-6_build-artifact
          path: packages/@cdklabs/cdk-amazonmq-activemq-config-v5-17-6/dist
          overwrite: true
      - name: "@cdklabs/cdk-amazonmq-activemq-config-v5-18-4: Backup artifact permissions"
        if: ${{ steps.git_remote.outputs.latest_commit == github.sha }}
        run: cd dist && getfacl -R . > permissions-backup.acl
        continue-on-error: true
        working-directory: packages/@cdklabs/cdk-amazonmq-activemq-config-v5-18-4
      - name: "@cdklabs/cdk-amazonmq-activemq-config-v5-18-4: Upload artifact"
        if: ${{ steps.git_remote.outputs.latest_commit == github.sha }}
        uses: actions/upload-artifact@v7
        with:
          name: cdklabs-cdk-amazonmq-activemq-config-v5-18-4_build-artifact
          path: packages/@cdklabs/cdk-amazonmq-activemq-config-v5-18-4/dist
          overwrite: true
      - name: "@cdklabs/cdk-amazonmq-activemq-config-v5-19-1: Backup artifact permissions"
        if: ${{ steps.git_remote.outputs.latest_commit == github.sha }}
        run: cd dist && getfacl -R . > permissions-backup.acl
        continue-on-error: true
        working-directory: packages/@cdklabs/cdk-amazonmq-activemq-config-v5-19-1
      - name: "@cdklabs/cdk-amazonmq-activemq-config-v5-19-1: Upload artifact"
        if: ${{ steps.git_remote.outputs.latest_commit == github.sha }}
        uses: actions/upload-artifact@v7
        with:
          name: cdklabs-cdk-amazonmq-activemq-config-v5-19-1_build-artifact
          path: packages/@cdklabs/cdk-amazonmq-activemq-config-v5-19-1/dist
          overwrite: true
  release_gate:
    name: Publish gate
    needs: release
    runs-on: ubuntu-latest
    permissions: {}
    outputs:
      packages_to_publish: ${{ steps.collect.outputs.packages_to_publish }}
    if: ${{ needs.release.outputs.latest_commit == github.sha }}
    steps:
      - name: "@cdklabs/cdk-amazonmq-activemq-config-v5-15-16: Check publish"
        run: |-
          should_publish="${{ needs.release.outputs.publish-cdklabs-cdk-amazonmq-activemq-config-v5-15-16 }}"
          package_filter="${{ (!github.event.inputs.package || github.event.inputs.package == '*all packages*' || contains(fromJSON('["@cdklabs/cdk-amazonmq-activemq-config-v5-15-16"]'), github.event.inputs.package)) }}"
          if [[ "$should_publish" != "true" ]]; then
            echo "⏭️ Skipping @cdklabs/cdk-amazonmq-activemq-config-v5-15-16: release tag already exists"
          elif [[ "$package_filter" != "true" ]]; then
            echo "⏭️ Skipping @cdklabs/cdk-amazonmq-activemq-config-v5-15-16: not selected for release"
          else
            echo "✅ Will publish @cdklabs/cdk-amazonmq-activemq-config-v5-15-16"
            echo "@cdklabs/cdk-amazonmq-activemq-config-v5-15-16" >> publish.txt
          fi
      - name: "@cdklabs/cdk-amazonmq-activemq-config-v5-16-7: Check publish"
        run: |-
          should_publish="${{ needs.release.outputs.publish-cdklabs-cdk-amazonmq-activemq-config-v5-16-7 }}"
          package_filter="${{ (!github.event.inputs.package || github.event.inputs.package == '*all packages*' || contains(fromJSON('["@cdklabs/cdk-amazonmq-activemq-config-v5-16-7"]'), github.event.inputs.package)) }}"
          if [[ "$should_publish" != "true" ]]; then
            echo "⏭️ Skipping @cdklabs/cdk-amazonmq-activemq-config-v5-16-7: release tag already exists"
          elif [[ "$package_filter" != "true" ]]; then
            echo "⏭️ Skipping @cdklabs/cdk-amazonmq-activemq-config-v5-16-7: not selected for release"
          else
            echo "✅ Will publish @cdklabs/cdk-amazonmq-activemq-config-v5-16-7"
            echo "@cdklabs/cdk-amazonmq-activemq-config-v5-16-7" >> publish.txt
          fi
      - name: "@cdklabs/cdk-amazonmq-activemq-config-v5-17-6: Check publish"
        run: |-
          should_publish="${{ needs.release.outputs.publish-cdklabs-cdk-amazonmq-activemq-config-v5-17-6 }}"
          package_filter="${{ (!github.event.inputs.package || github.event.inputs.package == '*all packages*' || contains(fromJSON('["@cdklabs/cdk-amazonmq-activemq-config-v5-17-6"]'), github.event.inputs.package)) }}"
          if [[ "$should_publish" != "true" ]]; then
            echo "⏭️ Skipping @cdklabs/cdk-amazonmq-activemq-config-v5-17-6: release tag already exists"
          elif [[ "$package_filter" != "true" ]]; then
            echo "⏭️ Skipping @cdklabs/cdk-amazonmq-activemq-config-v5-17-6: not selected for release"
          else
            echo "✅ Will publish @cdklabs/cdk-amazonmq-activemq-config-v5-17-6"
            echo "@cdklabs/cdk-amazonmq-activemq-config-v5-17-6" >> publish.txt
          fi
      - name: "@cdklabs/cdk-amazonmq-activemq-config-v5-18-4: Check publish"
        run: |-
          should_publish="${{ needs.release.outputs.publish-cdklabs-cdk-amazonmq-activemq-config-v5-18-4 }}"
          package_filter="${{ (!github.event.inputs.package || github.event.inputs.package == '*all packages*' || contains(fromJSON('["@cdklabs/cdk-amazonmq-activemq-config-v5-18-4"]'), github.event.inputs.package)) }}"
          if [[ "$should_publish" != "true" ]]; then
            echo "⏭️ Skipping @cdklabs/cdk-amazonmq-activemq-config-v5-18-4: release tag already exists"
          elif [[ "$package_filter" != "true" ]]; then
            echo "⏭️ Skipping @cdklabs/cdk-amazonmq-activemq-config-v5-18-4: not selected for release"
          else
            echo "✅ Will publish @cdklabs/cdk-amazonmq-activemq-config-v5-18-4"
            echo "@cdklabs/cdk-amazonmq-activemq-config-v5-18-4" >> publish.txt
          fi
      - name: "@cdklabs/cdk-amazonmq-activemq-config-v5-19-1: Check publish"
        run: |-
          should_publish="${{ needs.release.outputs.publish-cdklabs-cdk-amazonmq-activemq-config-v5-19-1 }}"
          package_filter="${{ (!github.event.inputs.package || github.event.inputs.package == '*all packages*' || contains(fromJSON('["@cdklabs/cdk-amazonmq-activemq-config-v5-19-1"]'), github.event.inputs.package)) }}"
          if [[ "$should_publish" != "true" ]]; then
            echo "⏭️ Skipping @cdklabs/cdk-amazonmq-activemq-config-v5-19-1: release tag already exists"
          elif [[ "$package_filter" != "true" ]]; then
            echo "⏭️ Skipping @cdklabs/cdk-amazonmq-activemq-config-v5-19-1: not selected for release"
          else
            echo "✅ Will publish @cdklabs/cdk-amazonmq-activemq-config-v5-19-1"
            echo "@cdklabs/cdk-amazonmq-activemq-config-v5-19-1" >> publish.txt
          fi
      - name: Collect packages to publish
        id: collect
        run: |-
          if [ -f publish.txt ]; then
            packages=$(jq -Rc '.' publish.txt | jq -sc '.')
          else
            packages='[]'
          fi
          echo "packages_to_publish=$packages" >> $GITHUB_OUTPUT
          echo "Publishing: $packages"
  cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-15-16: Release"
    needs: release_gate
    runs-on: ubuntu-latest
    permissions: {}
    if: ${{ contains(fromJSON(needs.release_gate.outputs.packages_to_publish), '@cdklabs/cdk-amazonmq-activemq-config-v5-15-16') }}
    steps:
      - run: echo "Releasing @cdklabs/cdk-amazonmq-activemq-config-v5-15-16..."
  cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release_github:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-15-16: Publish to GitHub Releases"
    needs:
      - cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release_npm
      - cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release_maven
      - cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release_pypi
      - cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release_nuget
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-15-16_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        if: ${{ !inputs.dry_run }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: errout=$(mktemp); gh release create $(cat dist/releasetag.txt) -R $GITHUB_REPOSITORY -F dist/changelog.md -t $(cat dist/releasetag.txt) --target $GITHUB_SHA 2> $errout && true; exitcode=$?; if [ $exitcode -ne 0 ] && ! grep -q "Release.tag_name already exists" $errout; then cat $errout; exit $exitcode; fi
  cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release_npm:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-15-16: Publish to npm"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    environment: release
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-15-16_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          NPM_DIST_TAG: latest
          NPM_REGISTRY: registry.npmjs.org
          NPM_CONFIG_PROVENANCE: "true"
          NPM_TRUSTED_PUBLISHER: "true"
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-npm
  cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release_maven:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-15-16: Publish to Maven Central"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release
    runs-on: ubuntu-latest
    permissions:
      contents: read
    environment: release
    steps:
      - uses: actions/setup-java@v5
        with:
          distribution: corretto
          java-version: "11"
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-15-16_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          MAVEN_SERVER_ID: central-ossrh
          MAVEN_GPG_PRIVATE_KEY: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }}
          MAVEN_GPG_PRIVATE_KEY_PASSPHRASE: ${{ secrets.MAVEN_GPG_PRIVATE_KEY_PASSPHRASE }}
          MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
          MAVEN_USERNAME: ${{ secrets.MAVEN_USERNAME }}
          MAVEN_STAGING_PROFILE_ID: ${{ secrets.MAVEN_STAGING_PROFILE_ID }}
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-maven
  cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release_pypi:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-15-16: Publish to PyPI"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment: release
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - uses: actions/setup-python@v6
        with:
          python-version: 3.x
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-15-16_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          PYPI_TRUSTED_PUBLISHER: "true"
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-pypi
  cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release_nuget:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-15-16: Publish to NuGet Gallery"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment: release
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 6.x
          cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-15-16_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          NUGET_TRUSTED_PUBLISHER: "true"
          NUGET_USERNAME: ${{ secrets.NUGET_USERNAME }}
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-nuget
  cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release_done:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-15-16: Release complete"
    needs:
      - cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release_github
      - cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release_npm
      - cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release_maven
      - cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release_pypi
      - cdklabs-cdk-amazonmq-activemq-config-v5-15-16_release_nuget
    runs-on: ubuntu-latest
    permissions: {}
    if: ${{ !cancelled() && !failure() }}
    steps:
      - run: echo "All publish jobs complete"
  cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-16-7: Release"
    needs: release_gate
    runs-on: ubuntu-latest
    permissions: {}
    if: ${{ contains(fromJSON(needs.release_gate.outputs.packages_to_publish), '@cdklabs/cdk-amazonmq-activemq-config-v5-16-7') }}
    steps:
      - run: echo "Releasing @cdklabs/cdk-amazonmq-activemq-config-v5-16-7..."
  cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release_github:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-16-7: Publish to GitHub Releases"
    needs:
      - cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release_npm
      - cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release_maven
      - cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release_pypi
      - cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release_nuget
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-16-7_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        if: ${{ !inputs.dry_run }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: errout=$(mktemp); gh release create $(cat dist/releasetag.txt) -R $GITHUB_REPOSITORY -F dist/changelog.md -t $(cat dist/releasetag.txt) --target $GITHUB_SHA 2> $errout && true; exitcode=$?; if [ $exitcode -ne 0 ] && ! grep -q "Release.tag_name already exists" $errout; then cat $errout; exit $exitcode; fi
  cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release_npm:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-16-7: Publish to npm"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    environment: release
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-16-7_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          NPM_DIST_TAG: latest
          NPM_REGISTRY: registry.npmjs.org
          NPM_CONFIG_PROVENANCE: "true"
          NPM_TRUSTED_PUBLISHER: "true"
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-npm
  cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release_maven:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-16-7: Publish to Maven Central"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release
    runs-on: ubuntu-latest
    permissions:
      contents: read
    environment: release
    steps:
      - uses: actions/setup-java@v5
        with:
          distribution: corretto
          java-version: "11"
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-16-7_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          MAVEN_SERVER_ID: central-ossrh
          MAVEN_GPG_PRIVATE_KEY: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }}
          MAVEN_GPG_PRIVATE_KEY_PASSPHRASE: ${{ secrets.MAVEN_GPG_PRIVATE_KEY_PASSPHRASE }}
          MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
          MAVEN_USERNAME: ${{ secrets.MAVEN_USERNAME }}
          MAVEN_STAGING_PROFILE_ID: ${{ secrets.MAVEN_STAGING_PROFILE_ID }}
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-maven
  cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release_pypi:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-16-7: Publish to PyPI"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment: release
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - uses: actions/setup-python@v6
        with:
          python-version: 3.x
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-16-7_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          PYPI_TRUSTED_PUBLISHER: "true"
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-pypi
  cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release_nuget:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-16-7: Publish to NuGet Gallery"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment: release
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 6.x
          cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-16-7_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          NUGET_TRUSTED_PUBLISHER: "true"
          NUGET_USERNAME: ${{ secrets.NUGET_USERNAME }}
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-nuget
  cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release_done:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-16-7: Release complete"
    needs:
      - cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release_github
      - cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release_npm
      - cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release_maven
      - cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release_pypi
      - cdklabs-cdk-amazonmq-activemq-config-v5-16-7_release_nuget
    runs-on: ubuntu-latest
    permissions: {}
    if: ${{ !cancelled() && !failure() }}
    steps:
      - run: echo "All publish jobs complete"
  cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-17-6: Release"
    needs: release_gate
    runs-on: ubuntu-latest
    permissions: {}
    if: ${{ contains(fromJSON(needs.release_gate.outputs.packages_to_publish), '@cdklabs/cdk-amazonmq-activemq-config-v5-17-6') }}
    steps:
      - run: echo "Releasing @cdklabs/cdk-amazonmq-activemq-config-v5-17-6..."
  cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release_github:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-17-6: Publish to GitHub Releases"
    needs:
      - cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release_npm
      - cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release_maven
      - cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release_pypi
      - cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release_nuget
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-17-6_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        if: ${{ !inputs.dry_run }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: errout=$(mktemp); gh release create $(cat dist/releasetag.txt) -R $GITHUB_REPOSITORY -F dist/changelog.md -t $(cat dist/releasetag.txt) --target $GITHUB_SHA 2> $errout && true; exitcode=$?; if [ $exitcode -ne 0 ] && ! grep -q "Release.tag_name already exists" $errout; then cat $errout; exit $exitcode; fi
  cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release_npm:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-17-6: Publish to npm"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    environment: release
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-17-6_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          NPM_DIST_TAG: latest
          NPM_REGISTRY: registry.npmjs.org
          NPM_CONFIG_PROVENANCE: "true"
          NPM_TRUSTED_PUBLISHER: "true"
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-npm
  cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release_maven:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-17-6: Publish to Maven Central"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release
    runs-on: ubuntu-latest
    permissions:
      contents: read
    environment: release
    steps:
      - uses: actions/setup-java@v5
        with:
          distribution: corretto
          java-version: "11"
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-17-6_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          MAVEN_SERVER_ID: central-ossrh
          MAVEN_GPG_PRIVATE_KEY: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }}
          MAVEN_GPG_PRIVATE_KEY_PASSPHRASE: ${{ secrets.MAVEN_GPG_PRIVATE_KEY_PASSPHRASE }}
          MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
          MAVEN_USERNAME: ${{ secrets.MAVEN_USERNAME }}
          MAVEN_STAGING_PROFILE_ID: ${{ secrets.MAVEN_STAGING_PROFILE_ID }}
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-maven
  cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release_pypi:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-17-6: Publish to PyPI"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment: release
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - uses: actions/setup-python@v6
        with:
          python-version: 3.x
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-17-6_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          PYPI_TRUSTED_PUBLISHER: "true"
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-pypi
  cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release_nuget:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-17-6: Publish to NuGet Gallery"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment: release
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 6.x
          cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-17-6_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          NUGET_TRUSTED_PUBLISHER: "true"
          NUGET_USERNAME: ${{ secrets.NUGET_USERNAME }}
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-nuget
  cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release_done:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-17-6: Release complete"
    needs:
      - cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release_github
      - cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release_npm
      - cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release_maven
      - cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release_pypi
      - cdklabs-cdk-amazonmq-activemq-config-v5-17-6_release_nuget
    runs-on: ubuntu-latest
    permissions: {}
    if: ${{ !cancelled() && !failure() }}
    steps:
      - run: echo "All publish jobs complete"
  cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-18-4: Release"
    needs: release_gate
    runs-on: ubuntu-latest
    permissions: {}
    if: ${{ contains(fromJSON(needs.release_gate.outputs.packages_to_publish), '@cdklabs/cdk-amazonmq-activemq-config-v5-18-4') }}
    steps:
      - run: echo "Releasing @cdklabs/cdk-amazonmq-activemq-config-v5-18-4..."
  cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release_github:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-18-4: Publish to GitHub Releases"
    needs:
      - cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release_npm
      - cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release_maven
      - cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release_pypi
      - cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release_nuget
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-18-4_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        if: ${{ !inputs.dry_run }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: errout=$(mktemp); gh release create $(cat dist/releasetag.txt) -R $GITHUB_REPOSITORY -F dist/changelog.md -t $(cat dist/releasetag.txt) --target $GITHUB_SHA 2> $errout && true; exitcode=$?; if [ $exitcode -ne 0 ] && ! grep -q "Release.tag_name already exists" $errout; then cat $errout; exit $exitcode; fi
  cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release_npm:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-18-4: Publish to npm"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    environment: release
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-18-4_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          NPM_DIST_TAG: latest
          NPM_REGISTRY: registry.npmjs.org
          NPM_CONFIG_PROVENANCE: "true"
          NPM_TRUSTED_PUBLISHER: "true"
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-npm
  cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release_maven:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-18-4: Publish to Maven Central"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release
    runs-on: ubuntu-latest
    permissions:
      contents: read
    environment: release
    steps:
      - uses: actions/setup-java@v5
        with:
          distribution: corretto
          java-version: "11"
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-18-4_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          MAVEN_SERVER_ID: central-ossrh
          MAVEN_GPG_PRIVATE_KEY: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }}
          MAVEN_GPG_PRIVATE_KEY_PASSPHRASE: ${{ secrets.MAVEN_GPG_PRIVATE_KEY_PASSPHRASE }}
          MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
          MAVEN_USERNAME: ${{ secrets.MAVEN_USERNAME }}
          MAVEN_STAGING_PROFILE_ID: ${{ secrets.MAVEN_STAGING_PROFILE_ID }}
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-maven
  cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release_pypi:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-18-4: Publish to PyPI"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment: release
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - uses: actions/setup-python@v6
        with:
          python-version: 3.x
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-18-4_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          PYPI_TRUSTED_PUBLISHER: "true"
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-pypi
  cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release_nuget:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-18-4: Publish to NuGet Gallery"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment: release
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 6.x
          cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-18-4_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          NUGET_TRUSTED_PUBLISHER: "true"
          NUGET_USERNAME: ${{ secrets.NUGET_USERNAME }}
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-nuget
  cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release_done:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-18-4: Release complete"
    needs:
      - cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release_github
      - cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release_npm
      - cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release_maven
      - cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release_pypi
      - cdklabs-cdk-amazonmq-activemq-config-v5-18-4_release_nuget
    runs-on: ubuntu-latest
    permissions: {}
    if: ${{ !cancelled() && !failure() }}
    steps:
      - run: echo "All publish jobs complete"
  cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-19-1: Release"
    needs: release_gate
    runs-on: ubuntu-latest
    permissions: {}
    if: ${{ contains(fromJSON(needs.release_gate.outputs.packages_to_publish), '@cdklabs/cdk-amazonmq-activemq-config-v5-19-1') }}
    steps:
      - run: echo "Releasing @cdklabs/cdk-amazonmq-activemq-config-v5-19-1..."
  cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release_github:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-19-1: Publish to GitHub Releases"
    needs:
      - cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release_npm
      - cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release_maven
      - cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release_pypi
      - cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release_nuget
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-19-1_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        if: ${{ !inputs.dry_run }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: errout=$(mktemp); gh release create $(cat dist/releasetag.txt) -R $GITHUB_REPOSITORY -F dist/changelog.md -t $(cat dist/releasetag.txt) --target $GITHUB_SHA 2> $errout && true; exitcode=$?; if [ $exitcode -ne 0 ] && ! grep -q "Release.tag_name already exists" $errout; then cat $errout; exit $exitcode; fi
  cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release_npm:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-19-1: Publish to npm"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    environment: release
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-19-1_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          NPM_DIST_TAG: latest
          NPM_REGISTRY: registry.npmjs.org
          NPM_CONFIG_PROVENANCE: "true"
          NPM_TRUSTED_PUBLISHER: "true"
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-npm
  cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release_maven:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-19-1: Publish to Maven Central"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release
    runs-on: ubuntu-latest
    permissions:
      contents: read
    environment: release
    steps:
      - uses: actions/setup-java@v5
        with:
          distribution: corretto
          java-version: "11"
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-19-1_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          MAVEN_SERVER_ID: central-ossrh
          MAVEN_GPG_PRIVATE_KEY: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }}
          MAVEN_GPG_PRIVATE_KEY_PASSPHRASE: ${{ secrets.MAVEN_GPG_PRIVATE_KEY_PASSPHRASE }}
          MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
          MAVEN_USERNAME: ${{ secrets.MAVEN_USERNAME }}
          MAVEN_STAGING_PROFILE_ID: ${{ secrets.MAVEN_STAGING_PROFILE_ID }}
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-maven
  cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release_pypi:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-19-1: Publish to PyPI"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment: release
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - uses: actions/setup-python@v6
        with:
          python-version: 3.x
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-19-1_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          PYPI_TRUSTED_PUBLISHER: "true"
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-pypi
  cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release_nuget:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-19-1: Publish to NuGet Gallery"
    needs: cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment: release
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          package-manager-cache: false
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 6.x
          cache: false
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          name: cdklabs-cdk-amazonmq-activemq-config-v5-19-1_build-artifact
      - name: Restore build artifact permissions
        run: cd dist && setfacl --restore=permissions-backup.acl
        continue-on-error: true
      - name: Release
        env:
          NUGET_TRUSTED_PUBLISHER: "true"
          NUGET_USERNAME: ${{ secrets.NUGET_USERNAME }}
          PUBLIB_DRYRUN: ${{ inputs.dry_run }}
        run: npx -p publib@latest publib-nuget
  cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release_done:
    name: "@cdklabs/cdk-amazonmq-activemq-config-v5-19-1: Release complete"
    needs:
      - cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release_github
      - cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release_npm
      - cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release_maven
      - cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release_pypi
      - cdklabs-cdk-amazonmq-activemq-config-v5-19-1_release_nuget
    runs-on: ubuntu-latest
    permissions: {}
    if: ${{ !cancelled() && !failure() }}
    steps:
      - run: echo "All publish jobs complete"
